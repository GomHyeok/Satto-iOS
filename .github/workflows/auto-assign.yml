name: 'Auto Assign'
on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  assign:
    name: Auto assign
    runs-on: ubuntu-latest
    steps:
      - name: Auto assign
        uses: kentaro-m/auto-assign-action@v2.0.0
